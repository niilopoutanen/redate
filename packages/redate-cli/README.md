# ReDate CLI

ReDate CLI is the backend used by the desktop app, available through terminal.
https://www.npmjs.com/package/redate-cli


## Installation

```npm install -g redate-cli```

## Usage

Process files or folders

```redate process <paths...>```

Check version and update if newer available

```redate update```

View current configuration

```redate config get```
