{
  "name": "redate",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "npm run redate -w redate-cli",
    "gui": "npm run electron -w redate",
    "package": "npm run package -w redate"
  }
}